overwrite: true
schema: "services/graphql/schema.gql"
documents: "services/graphql/docs/*.gql"
generates:
  services/graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-operations"
      - "typed-document-node"
